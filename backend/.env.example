# Azure SQL connection (set these as App Service application settings in production)
DB_HOST=your-server.database.windows.net
DB_PORT=1433
DB_USER=your-sql-user
DB_PASS=your-sql-password
DB_NAME=your-database
