[config]
# The GitHub Actions workflow (main_dev.yml) already runs npm install + build
# before deploying, so Kudu/Oryx should NOT rebuild the uploaded package again.
SCM_DO_BUILD_DURING_DEPLOYMENT=false
